#include<stdio.h>
#include<stdlib.h>



#include <stdio.h>

/*int findMax(int arr[], int size);

int main() {
    int arr[100], size, max;
    printf("Enter the size of the array: ");
    scanf("%d", &size);

    printf("Enter the elements of the array:\n");
    for(int i=0; i<size; i++) {
        scanf("%d", &arr[i]);
    }

    max = findMax(arr, size);
    printf("Maximum element of the array is %d\n", max);
    return 0;
}

int findMax(int arr[], int size) {
    static int max = 0;
    if(size == 0) {
        return max;
    }
    else {
        if(arr[size-1] > max) {
            max = arr[size-1];
        }
        return findMax(arr, size-1);
    }
}*/


/*int ortalama(int dizi[],int a){
	if(a<0)
		return 0;
		
	else
		return dizi[a]+ortalama(dizi,a-1);
	
}





int main(){
	int dizi[]={7,8,3};
	int n=sizeof(dizi)/sizeof(dizi[0]);
	int ort=ortalama(dizi,n-1);
	printf("ortalama:%d" ,ort/n);
}
*/




